{
  "name": "discord-ts-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "build/main.js",
  "scripts": {
    "build": "tsc",
    "serve": "node build/main.js",
    "start": "npm run build && npm run serve",
    "deploy": "npm run build && node build/deploy-commands.js"
  },
  "dependencies": {
    "@discordx/importer": "^1.3.0",
    "@discordx/utilities": "^7.0.5",
    "discord.js": "^14.21.0",
    "discordx": "^11.9.1",
    "dotenv": "^17.2.0",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.2"
  }
}
